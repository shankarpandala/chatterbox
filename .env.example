# Copy to .env (git-ignored) and fill in. Load it before running the pipeline:
#   set -a; source .env; set +a
# HuggingFace token — needed for gated datasets (e.g. ai4bharat/indicvoices_r)
# and higher Hub rate limits. Create one at https://huggingface.co/settings/tokens
HF_TOKEN=
